// Stuff to add to our current app

// Register person + secret
// Route to register (GET)
// Router to add the user (POST)

// User database-ish

// Modify the vault for a dynamic content

// A way to log out

// Login route (POST) authentication

// Delete the user

const password = "Hello!";

const hashedPassword = "$0912ow10ownmsa0ski10q918eq-0";

process.argv;

process.env;
